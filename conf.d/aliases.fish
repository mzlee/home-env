# Alias-like definitions

function l --description 'Alias for ls -AlF'
  ls -AlF
end

function la --description 'Alias for ls -AF'
  ls -AF
end

function ll --description 'Alias for ls -l'
  ls -l
end
